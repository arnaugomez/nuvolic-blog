---
pubDate: 2026-06-01
author: Arnau Gómez
title: Why AI needs a new programming language
description: LLMs need a new programming language, but not for building software."
image:
  url: "/src/images/blog/1.jpeg"
  alt: ""
tags: ["Programming language", "Systems"]
---

Recently, engineers at Vercel developed Zero, a low-level programming language designed for AI. While I was excited by the announcement, it shocked me to see how different their vision of "a programming language for AI" is to mine.

Zero is a systems programming language akin to C and Rust. Its language design and toolchain gives AI rich context and feedback: strongly typed, explicit errors, explicit effects, and a CLI utility that allows AI to inspect the program as a graph. It's ideal for complex low-level utilities like compilers and CLIs.

**My use case for "a programming language for AI" is different**. Since Vercel is not going to build it for me, let me explain.

## Why tool calling is not enough

Coding assistants are the most successful use case of AI. LLMs are similarly capable of automating boring tasks in other sectors of knowledge work: education, legal, accounting, etc.

So far, automations in these other sectors are done with 2 techniques: tool calling and MCPs. In tool calling, the AI is presented with a list of possible actions called "tools". When the AI generates its response it can include an instruction to perform a certain action, like fetch the homework assignment of a student in a learning management system. This instruction is referred to as a "tool call".

```
TODO example user and ai message with tool call

```

MCPs can be thought of as collections of tools that allow AI to connect to an external system. For example, the MCP of a task manager app exposes a set of tools that allow AI to retrieve and manage your tasks.

If you combine a large language model with a list of tools, you create an AI agent capable of interacting with its environment. It is widely believed that tool calling is the way to create AI agents capable of complex tasks. However, my experience in the field has shown me that this approach has several problems:

### Tools are inefficient

Suppose you tell your agent to copy all the items that you created last week and have not completed yet, and create a list with all of them.

With tool calling, the AI model has to take many steps. First, it generates a tool call for the `readItems` tool. 

```
TODO example readItems tool call

```

Then, it reads all the items. This information is loaded into the context window.

```
TODO example tool result truncated 2 items

```

Then, it calls the `createList` tool. As we mention, every time the AI calls a tool it has to generate its content. So, to create the list, it has to generate the items again. 

```
TODO example tool call truncated 2 items
```

A human would not do it this way. To a human, this would be equivalent to copying the text of each task by hand, or typing them word for word.

AI models do not complain about copying by hand, but they suffer in other ways. First, LLM usage is billed per token, so reading and generating long sequences of content is expensive. Moreover, LLMs are probabilistic machines so they may not copy the content exactly. A study by Microsoft showed that even the most capable models have a likelihood of corrupting the documents they work with.

By contrast, if a developer could write code to automate this task, it would look like this:

```ts
example code to automate this task
```

Cleaner and shorter than tool calling.

### Tools are not flexible enough

My example above is contrived: you could enhance the `readItems` tool a `createdAtAfter` parameter to filter by creation date. 

```
```

However, in real-world deployments, users employ agents as a way to search and manage tasks of any kind. This opens many questions: should users be able to filter by all properties? Should each property have a filter for values greater than, equal to, and smaller than a certain value? How are filters combined? Tool configuration gets more and more complex.

### Too many tools confuse the model.

Studies show that a model's performance decreases with the number of tools. An excessive number of available tools makes it more likely for the model to use the wrong tool. This contrasts with the growing of capabilities that are expected from AI assistants nowadays.

## The solution is code?

It´s not a new idea: companies like Cloudflare and Anthropic have arrived to the same conclusion. Models are already very good at writing code, so let´s allow them to write short programs that perform the tasks they'd otherwise do with tool calls.

Crucially, the goal of this code is not to build software. This is not application code, built to be enjoyed by users. It is throwaway code. It´s a script that runs once, for one user, to perform a single task.

But when looking at the existing scripting languages, most of them lack a set of features that would significantly benefit AI:

### Tools and MCPs as code APIs

### Granular review and approval

### Durable execution

### A controlled environment

### Token efficiency

### Rich context

## We need this

Speed

Sustainability, environment and economic impact
